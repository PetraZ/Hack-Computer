/** Initializes a new Avoid Balls game and starts running it. */
class Main {
    function void main() {
        var AvoidBalls game;
        let game = AvoidBalls.new();
        do game.run();
        do game.dispose();
        return;
    }
}
